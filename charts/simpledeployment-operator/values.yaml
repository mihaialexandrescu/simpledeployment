# Default values for simpledeployment-operator.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: simpledeployment
  tag: "v0.1"
